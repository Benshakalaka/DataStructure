用一个二维数组来存储一个图的邻接矩阵
当有n个顶点时，需存放n个顶点信息和n^2个弧信息的存储量
若考虑无向图的邻接矩阵的对称性 则可采用压缩存储的方式只存矩阵的下三角（或者上三角元素）

度：
对于无向图 顶点vi的度是邻接矩阵第i行(或第i列)的元素之和
对于有向图 顶点vi的出度OD（vi）为第i行元素之和 入度ID（vi）为第j列元素之和


个人看法：
既然是矩阵存储 那可以联系之前学过的矩阵的压缩存储  但操作起来会很麻烦