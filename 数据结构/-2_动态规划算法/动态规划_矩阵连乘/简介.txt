本题为计算机算法分析 动态规划第一题
程序并没有将最后结果写出来 因为时间不够。。。
但是精髓什么的都有了 根据输出的矩阵可寻得最优解

矩阵相乘最优算法首先想到的是穷举法，但穷举计算量太大，因此使用动态规划
① 分析最优解的结构
A[1,n]为最优解（当然，最外层两边价格括号就相当于一个矩阵），其矩阵子链A[1,k]和A[k+1,n]也为最优。若存在有一种A[1,k]计算方式需要的计算量更少，那么用此替换原子链，得到的A[1,n]则不是最优了，这是矛盾的。
因此，最优子结构性质是动用动态规划的求解的显著特征。

②建立递归关系。
③计算最优值。
④构造最优解